(* Maybe you give a quantity like

   r 1 'test'

   gives one bit, one alpha, one number, an ordering on 1 object, etc.

   r perm these are the words

   permute them randomly?
   *)

let () = Core.Command.run Cmd.command
